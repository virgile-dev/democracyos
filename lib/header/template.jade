#header-container
  a(href='#', id='toggleButton')
    - var headerFontColor = config.headerFontColor || '#fff';
    span.bar-icon(style='background-color: #{headerFontColor}')
    span.bar-icon(style='background-color: #{headerFontColor}')
    span.bar-icon(style='background-color: #{headerFontColor}')

    h1#logo
      a(href=config.homeLink || '#{config.subPath}/')
        span= config.organizationName

  .user-nav.btn-group
    .user
    - var color = config.headerFontColor;
    - var style = color ? 'color: ' + color : '';
    a(href='#{config.subPath}/signin', style=style).login.pull-right.anonymous-user
      i.fa.fa-sign-in
      span= t('header.signin')
    a(href='#{config.subPath}/signup', style=style).login.pull-right.anonymous-user
      i.fa.fa-user
      span= t('header.signup')
